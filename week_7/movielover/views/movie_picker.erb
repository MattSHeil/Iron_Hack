<!DOCTYPE html>
<html>
<head>
	<link rel="stylesheet" type="text/css" href="../style.css">	
	<title>Pick and Choose</title>
</head>
<body>
	
	<h1> <%= @the_question %> </h1>

	<h2> <%= @counter_x %> / <%= @counter_y %> </h2> 

	<main class="mainmatt">		
			
		<% @thechosenones.each  do | movies | %>
			<% if movies.year == @correctmovie.year%>
				<img src="<%= movies.poster %>" width="150px" onClick="alert('yay')">
			<% else %>
				<img src="<%= movies.poster %>" width="150px" onClick="alert('nay')">
			<% end %>
		<% end %>

	</main>

</body>
</html>