<%= form_for @project do | f |%>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<br>
	<%= f.label :description %>
	<%= f.text_area :description %>
	<br>
	<%= f.submit "Save" %>
	<button>
		<%= link_to 'back to projects', projects_path %>
	</button>
<% end %>