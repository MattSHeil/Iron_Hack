<h1>Here are all products being listed atm</h1>

<% @product.each do | sp | %>
	<ul>
		<li>
			<%= link_to sp.name, user_product_path(sp.user.id, sp.id) %> by <%= sp.user.name %>
		</li>
	</ul>
<% end %>

<button>
	<%= link_to 'Home', home_path %>
</button>