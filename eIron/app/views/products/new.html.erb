
<%= form_for [@user, @product] do | f | %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<br>
	<%= f.label :description %>
	<%= f.text_area :description %>
	<br>
	<%= f.label :deadline %>
	<%= f.date_field :deadline %>
	<br>
	<button>	
		<%= f.submit 'Create' %>
	</button>
<% end%>